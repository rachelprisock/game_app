<div class="container" style="width:50%; text-align: center;">
	<h1> Add a new review for </h1>

<ul>
	<% if flash[:error] %>
		<% flash[:error].each do |error| %>
			<li>
				<%= error %>
			</li>
		<% end %>
	<% end %>
</ul>

<%= render 'form' %>

<%= link_to 'Cancel', game_path(@game), class: 'btn btn-large btn-danger' %>

</div>

